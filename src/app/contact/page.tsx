import { EmptyStatePage } from '@/components/page/EmptyStatePage';

export default function ContactPage() {
  return <EmptyStatePage title="お問い合わせ / フィードバック" subtitle="問い合わせ窓口の空状態です。送信仕様は要件確定後に実装します。" badge="SCR-010" />;
}
