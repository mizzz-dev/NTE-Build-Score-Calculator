'use client';

import type { SlotType, StatKey } from '@/lib/score/types';
import type { ScoreOcrCandidate, ScoreOcrStatus } from '@/features/ocr/lib/scoreOcrAssist';

const SLOT_LABELS: Record<SlotType, string> = { cartridge: 'カートリッジ', module: 'モジュール', gear: 'ギア', console: 'コンソール' };

type Props = { status: ScoreOcrStatus; error: string | null; rawText: string; candidate: ScoreOcrCandidate | null; statKeyOptions: StatKey[]; onRawTextChange: (value: string) => void; onSelectImage: (file: File | null) => void; onUpdateCandidate: (next: ScoreOcrCandidate) => void; onApplyCandidate: () => void };

export function ScoreOcrAssistPanel({ status, error, rawText, candidate, statKeyOptions, onRawTextChange, onSelectImage, onUpdateCandidate, onApplyCandidate }: Props) {
  return <div className="rounded-lg border border-[var(--color-border)] bg-[var(--color-bg-elevated)] p-3 space-y-2"><p className="text-sm font-medium">OCR入力補助（/score限定）</p><p className="text-xs text-[var(--color-text-secondary)]">画像はブラウザ内のみで処理し、サーバー保存しません。OCR結果は一時ドラフトです。</p><input type="file" accept="image/png,image/jpeg,image/webp" onChange={(e) => onSelectImage(e.target.files?.[0] ?? null)} className="text-xs" /><textarea className="w-full rounded-md border border-[var(--color-border)] bg-[var(--color-bg-elevated)] p-2 text-xs" rows={3} placeholder="OCR生テキスト行を貼り付け（PoC連携）" value={rawText} onChange={(e) => onRawTextChange(e.target.value)} />{status === 'processing' && <p className="text-xs text-[var(--color-text-secondary)]">OCR処理中...</p>}{error && <p className="text-xs text-[var(--color-danger)]">{error}</p>}{candidate && <div className="text-xs space-y-1 rounded border border-[var(--color-border)] p-2"><label className="block">装備タイプ<select className="ml-2 rounded border border-[var(--color-border)]" value={candidate.slot ?? ''} onChange={(e) => onUpdateCandidate({ ...candidate, slot: (e.target.value || undefined) as SlotType | undefined })}><option value="">未解決</option>{Object.entries(SLOT_LABELS).map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select></label><div className="grid grid-cols-2 gap-1"><select className="rounded border border-[var(--color-border)]" value={candidate.mainStatKey ?? ''} onChange={(e) => onUpdateCandidate({ ...candidate, mainStatKey: (e.target.value || undefined) as StatKey | undefined, requiresManualMain: false })}><option value="">メインキー未解決</option>{statKeyOptions.map((key) => <option key={key} value={key}>{key}</option>)}</select><input className="rounded border border-[var(--color-border)] px-1" value={candidate.mainStatValue ?? ''} onChange={(e) => onUpdateCandidate({ ...candidate, mainStatValue: e.target.value, requiresManualMain: false })} placeholder="メイン値" /></div>{candidate.subStats.map((sub, idx) => <div key={idx} className="grid grid-cols-2 gap-1"><select className="rounded border border-[var(--color-border)]" value={sub.key ?? ''} onChange={(e) => { const next = [...candidate.subStats]; next[idx] = { ...sub, key: (e.target.value || undefined) as StatKey | undefined, requiresManual: false }; onUpdateCandidate({ ...candidate, subStats: next }); }}><option value="">サブ{idx + 1}キー未解決</option>{statKeyOptions.map((key) => <option key={key} value={key}>{key}</option>)}</select><input className="rounded border border-[var(--color-border)] px-1" value={sub.value} placeholder={`サブ${idx + 1}値`} onChange={(e) => { const next = [...candidate.subStats]; next[idx] = { ...sub, value: e.target.value, requiresManual: false }; onUpdateCandidate({ ...candidate, subStats: next }); }} /></div>)}{candidate.warnings.map((warning) => <p key={warning} className="text-[var(--color-danger)]">{warning}</p>)}<button type="button" className="rounded-md border border-[var(--color-border)] px-2 py-1 disabled:opacity-50" onClick={onApplyCandidate} disabled={candidate.requiresManualMain || candidate.subStats.some((s) => s.requiresManual)}>フォームへ反映</button></div>}</div>;
}
