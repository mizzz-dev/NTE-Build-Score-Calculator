# `/compare` OCR 月次監査ログテンプレート

- テンプレート作成日: 2026-05-15
- 初版対象Issue: #135
- 対象: 条件付き正式展開後の `/compare` OCR
- 記録方針: 匿名・集計値のみ（画像、個人情報、アカウント情報は保存しない）

## 1. 対象期間

- 監査対象期間: `<YYYY-MM-01> - <YYYY-MM-DD>`
- 集計実施日: `<YYYY-MM-DD>`
- 集計担当: `<担当ロールまたはチーム名>`

## 2. 計測対象画面

- 対象画面: `/compare`
- 対象導線: `OCR有効 / OCR無効（rollback時）`
- 備考: `<任意>`

## 3. KPI集計（匿名・集計値）

| 指標 | 実績値 | 通常しきい値 | 警戒しきい値 | rollbackしきい値 | 判定 |
|---|---:|---:|---:|---:|---|
| fallback率 | `<%>` | <= 8.5% | > 8.5%（2営業日連続） | > 10.0%（2営業日連続） | `<通常/警戒/rollback>` |
| 手動補正率 | `<%>` | <= 18.0% | > 18.0%（2営業日連続） | > 21.0%（2営業日連続） | `<通常/警戒/rollback>` |
| 比較前確認離脱率 | `<%>` | <= 4.0% | > 4.0%（2営業日連続） | > 5.0%（2営業日連続） | `<通常/警戒/rollback>` |
| A/B取り違え誤反映率 | `<%>` | 0.0% | > 0.0%（1件でも検知） | > 0.0%（1件でも検知） | `<通常/警戒/rollback>` |

## 4. 重大確認項目

- 未確認OCR値の比較計算混入有無: `<なし / あり（件数）>`
- 保存payload互換確認: `<OK / NG>`
- 共有URL互換確認: `<OK / NG>`
- ランキングpayload互換確認: `<OK / NG>`

### 4.1 互換確認メモ

- 実施日: `<YYYY-MM-DD>`
- 実施タイミング: `<月次締め / 警戒超過時 / rollback実施時 / 再開前>`
- 差分: `<なし / あり（内容）>`

## 5. しきい値判定

- 月次総合判定: `<通常 / 警戒 / rollback検討 / rollback実施>`
- 連続超過の有無: `<なし / あり（指標名・日数）>`
- 判断理由: `<記載>`

## 6. 発生した改善Issue

| 優先度 | Issue | 起票日 | 契機KPI | ステータス | 備考 |
|---|---|---|---|---|---|
| `<P0/P1/P2>` | `#<番号>` | `<YYYY-MM-DD>` | `<指標名>` | `<open/closed>` | `<任意>` |

## 7. rollback要否

- rollback要否: `<不要 / 要検討 / 実施>`
- 判定日: `<YYYY-MM-DD>`
- 判定根拠: `<記載>`

## 8. 次月アクション

1. `<アクション1>`
2. `<アクション2>`
3. `<アクション3>`

## 9. 非変更宣言（監査ログ共通）

本月次監査では以下を変更対象にしない。

- OCRアルゴリズム
- OCR信頼度計算
- DB / auth / infra / deployment
- 保存payload仕様
- 共有URL仕様
- ランキング仕様
- 画像保存方式
- 外部OCR API連携
