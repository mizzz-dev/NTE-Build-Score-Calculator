# Issue #83 作業ログ（/score OCR失敗時UX改善）

- 日付: 2026-05-13
- 対象: `/score` OCR入力補助UI（失敗時メッセージ、手動補正支援、再試行導線）

## 実施内容
1. `ScoreOcrAssistPanel` に「次に行うこと」ガイダンスを追加。
2. 未解決項目数（メイン/サブ）を可視化し、反映可否の理由を表示。
3. 低信頼度・未解決理由文言を行動可能な日本語へ強化。
4. OCR処理遅延時（エンジン読み込み中/処理中）の補足メッセージを追加。
5. `runScoreOcrAssist` のエラーメッセージを再試行/手動fallback/手動入力を含む文言へ更新。
6. `scoreOcrAssist` テストへ失敗時メッセージ検証を追加。

## 確認結果サマリ
- OCR候補未確定時に補正対象が判別可能。
- 未解決項目が残る場合、フォーム反映不可理由を表示。
- OCR失敗時に次アクションが明示される。
- payload仕様変更なし（保存・共有・ランキングの非混入方針を維持）。
